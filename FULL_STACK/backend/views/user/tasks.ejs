<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Your Tasks | Task Manager</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0-alpha1/dist/css/bootstrap.min.css" rel="stylesheet">
    <link href="https://fonts.googleapis.com/css2?family=Poppins:wght@300;400;600&display=swap" rel="stylesheet">
    <link rel="stylesheet" href="/css/styles.css">
    <style>
        body {
            font-family: 'Poppins', sans-serif;
            background: linear-gradient(135deg, #333, #1a1a1a);
            margin: 0;
            padding: 0;
        }
        .container {
            max-width: 800px;
            margin: 50px auto;
            padding: 20px;
            background: white;
            border-radius: 12px;
            box-shadow: 0 4px 15px rgba(0, 0, 0, 0.1);
        }
        .task-item {
            display: flex;
            justify-content: space-between;
            align-items: center;
            padding: 15px;
            border-bottom: 1px solid #ddd;
            border-radius: 6px;
            margin-bottom: 10px;
            background: #f8f9fa;
        }
        .task-item:last-child {
            border-bottom: none;
        }
        .task-item strong {
            font-size: 1.1rem;
        }
        .task-item em {
            font-size: 0.9rem;
            color: #6c757d;
        }
        .btn-sm {
            font-size: 0.8rem;
        }
        h1 {
            font-size: 2rem;
            text-align: center;
            margin-bottom: 30px;
            font-weight: 600;
        }
        .btn-primary, .btn-secondary {
            margin-bottom: 10px;
        }
        .no-tasks {
            text-align: center;
            color: #6c757d;
            font-size: 1rem;
            margin-top: 20px;
        }
        @media (max-width: 576px) {
            .container {
                margin: 20px;
                padding: 15px;
            }
            .task-item {
                flex-direction: column;
                align-items: flex-start;
            }
            .task-item button {
                margin-top: 10px;
            }
        }
    </style>
</head>
<body>
    <%- include('../partials/navbar', { user: user }) %> <!-- Navbar -->
    <div class="container">
        <h1>Your Tasks</h1>
        <a href="/tasks/new" class="btn btn-primary mb-3 w-100">Add New Task</a>
        <a href="/tasks" class="btn btn-secondary mb-3 w-100">Back</a>

        <% if (tasks.length > 0) { %>
            <ul class="list-group">
                <% tasks.forEach(task => { %>
                    <% console.log("Task Due Date:", task.due_date); %>
                    <li class="list-group-item task-item">
                        <div>
                            <strong><%= task.title %></strong> 
                            <span>- <%= task.description %></span><br>
                            <em style="color: #333;">Due: <%= task.due_date ? new Date(task.due_date).toLocaleDateString() : 'No due date' %></em>
                        </div>
                        <form action="/tasks/delete/<%= task.id %>" method="POST" style="margin: 0;">
                            <button type="submit" class="btn btn-danger btn-sm">Delete</button>
                        </form>
                    </li>
                <% }) %>
            </ul>
        <% } else { %>
            <p class="no-tasks">No tasks available. Click "Add New Task" to get started.</p>
        <% } %>
    </div>

    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0-alpha1/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>